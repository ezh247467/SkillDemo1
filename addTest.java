import static org.junit.Assert.assertEquals;
// Time to test pull
//Test pull again!
import org.junit.*;
public class addTest {
    @Test
    public void addTwo() {
        assertEquals(5, add.addTwo(2, 3));
    }
}
